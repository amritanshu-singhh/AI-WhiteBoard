// Point this at your running backend (see backend/server.js).
// Defaults to same host on port 5000, which works for local development.
const API_BASE = `${window.location.protocol}//${window.location.hostname}:5000`;
